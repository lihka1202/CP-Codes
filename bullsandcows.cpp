#include <bits/stdc++.h>
using namespace std;

string getHint(string secret, string guess) {
    int bulls, cows = 0;
    unordered_set<char> secret_set;
    unordered_map<int,int> guess_mapping;
        
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    

    return 0;
}